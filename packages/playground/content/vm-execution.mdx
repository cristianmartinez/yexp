import { VMExecutionDemo } from '../components/vm-execution-demo'

export const metadata = {
  title: 'VM Execution Visualizer',
  description: 'Watch how the stack-based VM executes bytecode step-by-step'
}

# VM Execution Visualizer

An interactive visualization showing how Jext's stack-based virtual machine executes bytecode instructions in real-time.

## How It Works

The Jext VM is a **stack-based virtual machine** that:
1. Reads bytecode instructions sequentially
2. Uses an **instruction pointer (IP)** to track position
3. Manages a **stack** for intermediate values
4. Supports **jumps** for control flow (conditionals, loops)

## Try It Out!

Enter any Jext expression below and watch how the VM executes it step-by-step:

<VMExecutionDemo
  initialExpression="1 + 2 * 3"
/>

## Simple Arithmetic

Try changing the expression above to see how different operations work. Here are some examples to try:

- `1 + 2 * 3` - See operator precedence in action
- `(1 + 2) * 3` - Watch how parentheses change evaluation order
- `5 - 3 + 2` - Left-to-right evaluation
- `-10 + 5` - Unary negation

## Conditional Jumps

Try a ternary operator to see how the VM uses jump instructions:

<VMExecutionDemo
  initialExpression="data.x > 10 ? 'big' : 'small'"
  initialContext={`{
  "data": { "x": 15 },
  "state": {},
  "env": {}
}`}
/>

**Jump Instructions** - Watch the instruction pointer (IP) jump around:
- `JUMP_IF_FALSE` - Jump if condition is false
- `JUMP_IF_TRUE` - Jump if condition is true
- `JUMP` - Unconditional jump

The IP doesn't just increment - it can jump forward or backward based on the bytecode!

## Complex Expression

Watch a real-world example that combines multiple features - filtering, mapping, and string templates:

<VMExecutionDemo
  initialExpression="data.users |> filter(u => u.age >= 18 && u.active) |> map(u => `${u.name} (${u.age})`) |> join(', ')"
  initialContext={`{
  "data": {
    "users": [
      { "name": "Alice", "age": 25, "active": true },
      { "name": "Bob", "age": 17, "active": true },
      { "name": "Carol", "age": 30, "active": false },
      { "name": "Dave", "age": 22, "active": true }
    ]
  },
  "state": {},
  "env": {}
}`}
/>

**What's Happening:**
1. `filter` - Creates lambda, evaluates condition for each user
2. Multiple `JUMP` instructions - For each lambda invocation
3. `map` - Another lambda with template literal evaluation
4. `join` - Final transformation to string
5. Stack grows/shrinks as lambdas execute

Watch the IP jump into and out of lambda functions!

## Object Construction with Spread

See how objects and arrays are built on the stack:

<VMExecutionDemo
  initialExpression="{ ...data.defaults, name: data.user.name, score: data.scores[0] * 2, status: data.active ? 'online' : 'offline' }"
  initialContext={`{
  "data": {
    "defaults": { "color": "blue", "size": "medium" },
    "user": { "name": "Alice" },
    "scores": [45, 38, 42],
    "active": true
  },
  "state": {},
  "env": {}
}`}
/>

**Stack Mechanics:**
- Values are pushed in reverse order (right to left)
- `SPREAD` operator flattens objects
- `MAKE_OBJ` pops all values and constructs object
- Each property evaluation pushes 2 values (key, value)

## More Examples to Try

**Array Operations:**
```
[1, 2, 3][1]
```

**Object Access with Optional Chaining:**
```
data.user?.profile?.avatar ?? "default.png"
```

**Logical Operations:**
```
data.age > 18 && data.verified
```

**String Template:**
```
`Hello ${data.name}, you have ${data.items.length} items`
```

**Nested Ternary (Complex Control Flow):**
```
data.score >= 90 ? "A" : data.score >= 80 ? "B" : data.score >= 70 ? "C" : "F"
```

**Recursive Descent (Deep Property Access):**
```
data..price[. > 100]
```

## Stack Operations

The stack is a **LIFO (Last In, First Out)** data structure:

- **Push**: Add value to top of stack
- **Pop**: Remove and return top value
- **Binary ops**: Pop two values, push result
- **Unary ops**: Pop one value, push result

## How to Use

1. **Enter Expression** - Type any Jext expression in the editor
2. **Set Context** - Switch to the Context tab to add data/state/env
3. **Watch Execution** - The bytecode and stack update in real-time
4. **Control Playback:**
   - Click **Play** to auto-step through execution
   - Use **Step Forward/Backward** to manually control
   - **Reset** to start over

## Understanding the Visualization

**Left Panel - Bytecode Instructions:**
- Blue highlight shows current instruction
- Dimmed text shows already-executed instructions
- Jump targets show where control flow goes

**Right Panel - Stack:**
- Shows current stack state (top at bottom)
- Values animate in/out as they're pushed/popped
- Final result appears when execution completes

This is a **stack-based VM** - all operations work through the stack. No registers, just push and pop!
