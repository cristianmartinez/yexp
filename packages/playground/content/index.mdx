import { VMExecutionDemo } from '../components/vm-execution-demo'

# Yexp Documentation

Welcome to **Yexp** — a fast expression language with JavaScript-like syntax and query-language superpowers.

## VM Execution Visualizer

Watch how expressions execute step-by-step in the bytecode virtual machine:

<VMExecutionDemo
  initialExpression="$.items |> filter(.price < 100) |> map(.name) |> join(', ')"
  initialContext={`{
  "items": [
    { "name": "Laptop", "price": 999 },
    { "name": "Mouse", "price": 25 },
    { "name": "Keyboard", "price": 75 },
    { "name": "Monitor", "price": 350 },
    { "name": "USB Cable", "price": 12 }
  ]
}`}
/>

## What is Yexp?

Yexp is an expression engine designed for:
- **Safe evaluation** - No arbitrary code execution
- **Predictable semantics** - Explicit input, context, and environment
- **High performance** - Compiles to bytecode for fast execution
- **Familiar syntax** - JavaScript-inspired expressions with quick-query selectors

## Quick Example

```javascript
// Filter and transform data
$.products
  .filter(p => p.price < 100)
  .map(p => p.name)
  .join(", ")
```

## Key Features

- ✓ Property access & optional chaining
- ✓ Array methods (filter, map, reduce, etc.)
- ✓ Higher-order functions & lambdas
- ✓ Template literals
- ✓ Pipe operator for composition
- ✓ Method chaining syntax
- ✓ Spread operators
- ✓ Predicate selectors, wildcards, negative indices, and recursive descent

## Learn More

- [Getting Started](/docs/getting-started) - Install and use Yexp
- [Language Guide](/docs/syntax) - Learn Yexp from simple expressions to advanced queries
- [Architecture](/docs/architecture) - How the VM works
